<!DOCTYPE html>
<html>
  <head>
    <title>Tasker</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
  <% if current_user.present? %>
    <div style="position: fixed; top: 10px; right: 1000px;">
      <%= button_to "Sign Out", destroy_user_session_path, method: :delete, class: 'box-sizing: border-box; font-size: 14px; margin: 0;' %>
    </div>
  <% else %>   
    <%= button_to "Sign Up", new_user_registration_path, method: :post, class: 'box-sizing: border-box; font-size: 14px; margin: 0;' %>
  <% end %>
    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>
    <%= yield %>
</body>

